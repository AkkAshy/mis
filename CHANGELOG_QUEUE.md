# Changelog - Система очередей пациентов

## Версия 3.0 - Очередь через приемы (18.11.2025)

### ✨ Изменения

#### Перенос логики очереди из регистрации пациента в создание приема
- **До:** Пациент регистрировался с указанием врача (`doctor_id`), очередь создавалась сразу
- **После:** Пациент регистрируется БЕЗ врача, затем создается прием к врачу, и ТАМ создается очередь
- **Файлы:**
  - [app/schemas/patient.py](app/schemas/patient.py) - убрано поле `doctor_id` из PatientCreate
  - [app/routes/patients.py](app/routes/patients.py) - убрана логика очереди
  - [app/routes/appointments.py:139-180](app/routes/appointments.py) - добавлена логика очереди

**Новый workflow:**
```
1. POST /patients/ (БЕЗ doctor_id)
   → Создается пациент

2. POST /appointments/ (С doctor_id и patient_id)
   → Создается прием
   → Автоматически добавляется в очередь (FIFO - в конец)
```

**Пример:**
```json
# Шаг 1: Регистрация пациента
POST /patients/
{
  "full_name": "Иванов Иван",
  "birth_date": "1990-01-01",
  "gender": "male",
  "phone": "+7 123 456 7890"
}

Результат: patient_id = 15

# Шаг 2: Создание приема
POST /appointments/
{
  "patient_id": 15,
  "doctor_id": 2,
  "date": "2025-11-18",
  "notes": "Первичный осмотр",
  "cost": 1500
}

Результат:
- Прием создан
- Пациент автоматически добавлен в очередь врача #2
- Если у врача было 6 пациентов, новый получает номер 7 (FIFO)
```

### ⚠️ Breaking Changes

**ДА** - изменился workflow:

**Старый workflow (НЕ работает):**
```json
POST /patients/
{
  "full_name": "...",
  "doctor_id": 2  ← Больше не поддерживается!
}
```

**Новый workflow:**
```json
# 1. Регистрация (БЕЗ врача)
POST /patients/
{
  "full_name": "..."
}

# 2. Создание приема (С врачом)
POST /appointments/
{
  "patient_id": 15,
  "doctor_id": 2,
  ...
}
```

---

## Версия 2.1 - Выбор врача (18.11.2025) [УСТАРЕЛО]

### ✨ Изменения

#### Добавлен обязательный параметр `doctor_id` при регистрации пациента
- **До:** Пациент добавлялся в очередь ко всем врачам автоматически
- **После:** Reception выбирает конкретного врача через параметр `doctor_id`
- **Файлы:**
  - [app/schemas/patient.py:13](app/schemas/patient.py) - добавлено поле `doctor_id`
  - [app/routes/patients.py:33-68](app/routes/patients.py) - обновлена логика

**Пример:**
```json
POST /patients/
{
  "full_name": "Иванов Иван",
  "birth_date": "1990-01-01",
  "gender": "male",
  "phone": "+7 123 456 7890",
  "doctor_id": 2  ← ОБЯЗАТЕЛЬНЫЙ параметр
}

Результат:
- Пациент добавлен только к врачу #2
- Получает номер 1 в очереди врача #2
- Остальные врачи: без изменений
```

### 📝 Обновления API

**Изменено:**
- `POST /patients/` - добавлен обязательный параметр `doctor_id`

**Схема PatientCreate:**
```python
class PatientCreate(BaseModel):
    full_name: str
    birth_date: date
    gender: str
    phone: str
    passport: Optional[str] = None
    address: Optional[str] = None
    doctor_id: int  # ← НОВОЕ поле
```

### ⚠️ Breaking Changes

**ДА** - теперь требуется параметр `doctor_id` при создании пациента.

**Миграция:**
Если у вас уже есть код, который создает пациентов, добавьте параметр `doctor_id`:
```diff
POST /patients/
{
  "full_name": "...",
  "birth_date": "...",
  "gender": "...",
  "phone": "...",
+ "doctor_id": 2
}
```

---

## Версия 2.0 - Обновление (18.11.2025) [УСТАРЕЛО]

### ✨ Новые возможности

#### 1. Автоматическое создание очереди при регистрации пациента [УСТАРЕЛО - см. версию 3.0]
- **До:** Reception должен был вручную добавлять пациента в очередь через `POST /queue/`
- **После:** При регистрации пациента (`POST /patients/`) он **автоматически** добавляется в очередь ко всем врачам
- **Файл:** [app/routes/patients.py:39-63](app/routes/patients.py)
- **ПРИМЕЧАНИЕ:** Эта логика изменена в версии 3.0 - теперь очередь создается при создании приема

**Пример:**
```python
# Reception регистрирует пациента
POST /patients/
{
  "full_name": "Иванов Иван",
  "birth_date": "1990-01-01",
  "gender": "male",
  "phone": "+7 123 456 7890"
}

# Автоматически создаются записи в очереди:
# - Врач #1: пациент получает номер 1
# - Врач #2: пациент получает номер 1
# - Врач #3: пациент получает номер 1
# Все существующие пациенты сдвигаются вниз
```

#### 2. LIFO для списка пациентов
- **До:** Пациенты показывались в порядке регистрации (старые первыми)
- **После:** Последний зарегистрированный пациент показывается **первым**
- **Файл:** [app/routes/patients.py:57-58](app/routes/patients.py)

**Пример:**
```bash
# Регистрируем пациентов:
1. Петров (ID: 100)
2. Сидоров (ID: 101)
3. Иванов (ID: 102)

# GET /patients/?skip=0&limit=10
# Результат:
[
  {id: 102, name: "Иванов"},    # ← Последний зарегистрированный
  {id: 101, name: "Сидоров"},
  {id: 100, name: "Петров"}
]
```

**Преимущество:** Новый пациент сразу виден на первой странице, а не на 6-й странице как раньше.

### 🔧 Технические детали

#### Изменения в коде

**1. app/routes/patients.py**
```python
# Добавлена сортировка LIFO
query = query.order_by(PatientModel.id.desc())

# Автоматическое добавление в очередь
doctors = db.query(User).filter(User.role == "doctor").all()
for doctor in doctors:
    # Сдвигаем существующие номера
    db.query(QueueModel).filter(...).update({"queue_number": QueueModel.queue_number + 1})
    # Добавляем нового пациента с номером 1
    new_queue_entry = QueueModel(patient_id=db_patient.id, doctor_id=doctor.id, queue_number=1, ...)
```

**2. Независимость очередей**
Очереди всегда были независимы для каждого врача (логика уже была правильной):
```python
# app/routes/queue.py:51-56
db.query(QueueModel).filter(
    and_(
        QueueModel.doctor_id == queue_data.doctor_id,  # ← Фильтр по врачу
        QueueModel.queue_date == today
    )
).update({"queue_number": QueueModel.queue_number + 1})
```

### 📊 Примеры работы

#### Пример 1: Независимые очереди врачей

**Состояние очередей:**
```
Врач #1:
  1. Петров
  2. Сидоров

Врач #2:
  1. Иванов
  2. Васильев
```

**Reception регистрирует нового пациента "Кузнецов":**

**Результат:**
```
Врач #1:
  1. Кузнецов  ← Новый
  2. Петров    ← Сдвинулся
  3. Сидоров   ← Сдвинулся

Врач #2:
  1. Кузнецов  ← Новый
  2. Иванов    ← Сдвинулся
  3. Васильев  ← Сдвинулся
```

**Важно:** Нумерация **НЕ** общая (1,2,3,4,5,6), а независимая для каждого врача (1,2,3 и 1,2,3).

### 🚀 Миграция с предыдущей версии

Если у вас уже есть существующие пациенты без очередей:

1. Обновите код
2. Запустите сервер
3. Новые пациенты будут автоматически добавляться в очереди
4. Старые пациенты можно добавить вручную через `POST /queue/` или оставить как есть

### 📝 API Changes

#### Изменено поведение:
- `POST /patients/` - теперь автоматически создает записи в очереди

#### Новые параметры:
- Нет новых параметров

#### Deprecated:
- Ничего не устарело, `POST /queue/` все еще работает для ручного добавления

### ⚠️ Breaking Changes

**НЕТ** - все изменения обратно совместимы.

### 🐛 Исправления

- Исправлено: Новые пациенты попадали на последние страницы списка
- Улучшено: Reception больше не нужно вручную добавлять пациентов в очередь

### 📚 Обновленная документация

- [QUEUE_README.md](QUEUE_README.md) - полная документация системы очередей
- Добавлены новые сценарии использования
- Обновлены примеры

---

## Версия 1.0 - Первый релиз (18.11.2025)

### ✨ Реализовано

1. Модель Queue для хранения очередей
2. API endpoints для управления очередями
3. LIFO логика для очередей
4. Автоматический сброс очередей в 00:00
5. Интеграция с /patients для отображения номеров очереди
6. Независимые очереди для каждого врача
7. Планировщик задач APScheduler

### 📦 Зависимости

- apscheduler==3.10.4

### 🗄️ Миграции БД

- Создана таблица `queue`
- Миграция: `1c93d15527cb_add_queue_table.py`
