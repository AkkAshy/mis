# Система очередей пациентов

## Описание

Система очередей позволяет управлять очередями пациентов для каждого врача отдельно. Очереди автоматически сбрасываются каждый день в 00:00.

## Ключевые особенности

### 1. LIFO (Last In First Out)
- **Новые пациенты добавляются в начало очереди** с номером 1
- Все существующие пациенты автоматически сдвигаются вниз (+1 к номеру)
- Это позволяет приоритизировать последних добавленных пациентов
- **Список пациентов (`/patients`) также отсортирован по LIFO** - последний зарегистрированный показывается первым

### 2. Автоматическое добавление в очередь
- **При регистрации нового пациента** он автоматически добавляется в очередь ко всем врачам
- Reception не нужно вручную добавлять пациента в очередь
- Пациент сразу получает номер 1 у каждого врача

### 3. Независимые очереди для врачей
- Каждый врач имеет свою независимую очередь
- Если у врача #1 два пациента (1, 2) и у врача #2 два пациента - нумерация будет (1, 2) для каждого
- **НЕ** общая нумерация (1, 2, 3, 4)

### 4. Уникальность
- Каждая очередь уникальна для конкретного врача
- Пациент не может быть добавлен в очередь одного врача дважды в один день

### 5. Автоматический сброс
- Все старые очереди автоматически удаляются каждый день в 00:00
- Используется планировщик APScheduler

## API Endpoints

### 📌 POST `/queue/` - Добавить пациента в очередь
Добавляет пациента в очередь врача (LIFO).

**Request Body:**
```json
{
  "patient_id": 1,
  "doctor_id": 2
}
```

**Response:**
```json
{
  "id": 1,
  "patient_id": 1,
  "doctor_id": 2,
  "queue_number": 1,
  "queue_date": "2025-11-18",
  "created_at": "2025-11-18T15:30:00"
}
```

### 📋 GET `/queue/doctor/{doctor_id}` - Получить очередь врача
Возвращает список пациентов в очереди врача.

**Query Parameters:**
- `queue_date` (optional): Дата очереди (по умолчанию: сегодня)

**Response:**
```json
{
  "queue": [
    {
      "id": 3,
      "patient_id": 5,
      "doctor_id": 2,
      "queue_number": 1,
      "queue_date": "2025-11-18",
      "created_at": "2025-11-18T15:30:00",
      "patient_full_name": "Иванов Иван Иванович",
      "patient_phone": "+7 123 456 7890"
    }
  ],
  "total_count": 1
}
```

### 🗑️ DELETE `/queue/{queue_id}` - Удалить из очереди
Удаляет пациента из очереди. Номера следующих пациентов автоматически уменьшаются.

### 🧹 POST `/queue/clear/{doctor_id}` - Очистить очередь врача
Очищает всю очередь конкретного врача на указанную дату.

**Query Parameters:**
- `queue_date` (optional): Дата очереди (по умолчанию: сегодня)

**Требуется роль:** `admin` или `doctor`

### 🔄 POST `/queue/reset-all` - Сбросить все очереди
Удаляет все старые очереди (старше сегодняшней даты).

**Требуется роль:** `admin`

## Интеграция с `/patients`

### GET `/patients/` - Список пациентов с номерами очереди

**Новый параметр:**
- `doctor_id` (optional): Если указан, каждый пациент будет содержать поле `queue_number`

**Пример:**
```
GET /patients/?skip=0&limit=10&doctor_id=2
```

**Response:**
```json
{
  "patients": [
    {
      "id": 1,
      "patient_uid": "...",
      "full_name": "Петров Петр Петрович",
      "birth_date": "1990-01-01",
      "gender": "male",
      "phone": "+7 123 456 7890",
      "passport": "1234 567890",
      "address": "Москва",
      "created_at": "2025-11-18T10:00:00",
      "queue_number": 2
    }
  ],
  "total_count": 100
}
```

**Примечание:** Поле `queue_number` будет `null`, если:
- Параметр `doctor_id` не указан
- Пациент не находится в очереди этого врача сегодня

## Структура БД

### Таблица `queue`

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Primary Key |
| patient_id | Integer | FK → patients.id |
| doctor_id | Integer | FK → users.id |
| queue_number | Integer | Номер в очереди (1, 2, 3...) |
| queue_date | Date | Дата очереди |
| created_at | DateTime | Время добавления в очередь |

## Примеры использования

### Сценарий 1: Регистрация нового пациента (автоматическое добавление в очередь)

**Reception регистрирует нового пациента:**
```json
POST /patients/
{
  "full_name": "Иванов Иван Иванович",
  "birth_date": "1990-01-01",
  "gender": "male",
  "phone": "+7 123 456 7890"
}
```

**Что происходит автоматически:**
1. Пациент создается в базе данных
2. Автоматически добавляется в очередь ко **всем** врачам с номером **1**
3. Все существующие пациенты в очереди каждого врача сдвигаются вниз (+1)

**Результат:**
- Врач #1: Новый пациент получает номер 1, все остальные сдвигаются
- Врач #2: Новый пациент получает номер 1, все остальные сдвигаются
- Врач #3: Новый пациент получает номер 1, все остальные сдвигаются

### Сценарий 2: Ручное добавление пациента в очередь (опционально)

1. Добавляем пациента #1 к врачу #2:
   ```json
   POST /queue/
   {"patient_id": 1, "doctor_id": 2}
   ```
   Результат: Пациент #1 получает номер **1**

2. Добавляем пациента #2 к врачу #2:
   ```json
   POST /queue/
   {"patient_id": 2, "doctor_id": 2}
   ```
   Результат:
   - Пациент #2 получает номер **1**
   - Пациент #1 сдвигается на номер **2**

3. Добавляем пациента #3 к врачу #2:
   ```json
   POST /queue/
   {"patient_id": 3, "doctor_id": 2}
   ```
   Результат:
   - Пациент #3 получает номер **1**
   - Пациент #2 сдвигается на номер **2**
   - Пациент #1 сдвигается на номер **3**

### Сценарий 3: Просмотр пациентов с номерами очереди

**Получить список пациентов (LIFO - последний зарегистрированный первым):**
```
GET /patients/?skip=0&limit=10
```

Пациенты отсортированы по ID в обратном порядке - последний зарегистрированный будет первым в списке.

**Получить список с номерами очереди для конкретного врача:**
```
GET /patients/?doctor_id=2&skip=0&limit=10
```

Каждый пациент в ответе будет содержать свой номер очереди у врача #2 в поле `queue_number`.

### Сценарий 4: Удаление из очереди

```
DELETE /queue/5
```

Если пациент имел номер 2, то все пациенты с номерами > 2 автоматически сдвинутся вверх (-1 к номеру).

## Миграция базы данных

Для применения изменений в БД:

```bash
source venv/bin/activate
alembic upgrade head
```

## Зависимости

Добавлена новая зависимость:
- `apscheduler==3.10.4` - для автоматического сброса очередей

Установка:
```bash
pip install -r requirements.txt
```

## Логирование

Планировщик логирует следующие события:
- ✓ Запуск планировщика при старте приложения
- ✓ Успешный сброс очередей в 00:00 (с количеством удаленных записей)
- ✗ Ошибки при сбросе очередей

## Тестирование

Для тестирования функциональности:

1. Запустите сервер:
   ```bash
   uvicorn app.main:app --reload
   ```

2. Откройте Swagger UI:
   ```
   http://localhost:8000/docs
   ```

3. Используйте раздел "queue" для тестирования эндпоинтов
