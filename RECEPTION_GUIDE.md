# Инструкция для Reception - Система очередей

## 🎯 Что изменилось?

### Раньше:
1. Регистрировали пациента через `POST /patients/`
2. **Вручную** добавляли его в очередь к врачу через `POST /queue/`

### Теперь:

1. Регистрируете пациента через `POST /patients/` (БЕЗ указания врача)
2. Создаете прием к врачу через `POST /appointments/` (с указанием врача)
3. **ВСЁ!** Пациент автоматически добавляется в очередь при создании приема

## 📝 Как работать с пациентами

### Шаг 1: Регистрация пациента

**Endpoint:** `POST /patients/`

**Пример запроса:**
```json
{
  "full_name": "Иванов Иван Иванович",
  "birth_date": "1990-01-01",
  "gender": "male",
  "phone": "+7 123 456 7890",
  "passport": "1234 567890",
  "address": "г. Бишкек, ул. Ленина 1"
}
```

**ВАЖНО:** НЕ указываем врача при регистрации пациента!

**Что происходит:**

- ✅ Пациент создается в системе
- ℹ️ Пациент пока НЕ в очереди ни у одного врача

### Шаг 2: Создание приема к врачу

**Endpoint:** `POST /appointments/`

**Пример запроса:**

```json
{
  "patient_id": 15,
  "doctor_id": 2,
  "date": "2025-11-18",
  "notes": "Первичный осмотр",
  "cost": 1500
}
```

**Что происходит автоматически:**

- ✅ Создается прием у врача
- ✅ Пациент автоматически добавляется в очередь к этому врачу
- ✅ Пациент получает следующий номер в очереди (если у врача 6 пациентов, новый получит номер 7)

**Вы больше НЕ ДОЛЖНЫ:**

- ❌ Вручную добавлять пациента в очередь после создания приема
- ❌ Использовать `POST /queue/` для пациентов с приемами (используется только для исключительных случаев)

## 📊 Просмотр пациентов

### Список всех пациентов

**Endpoint:** `GET /patients/?skip=0&limit=10`

**Важно:**
- Пациенты показываются **в обратном порядке** (последний зарегистрированный — первый)
- Новый пациент всегда будет на **первой странице**
- Не нужно листать до конца, чтобы найти последнего пациента

**Пример:**
```
Зарегистрировали пациентов:
1. Петров (10:00)
2. Сидоров (11:00)
3. Иванов (12:00)

GET /patients/?skip=0&limit=10
Результат:
1. Иванов ← Последний (показывается первым!)
2. Сидоров
3. Петров
```

### Список пациентов с номерами очереди

**Endpoint:** `GET /patients/?doctor_id=2&skip=0&limit=10`

**Параметры:**
- `doctor_id` - ID врача (обязательно для показа номеров очереди)
- `skip` - сколько пациентов пропустить (для пагинации)
- `limit` - сколько пациентов показать (максимум 100)

**Пример ответа:**
```json
{
  "patients": [
    {
      "id": 15,
      "full_name": "Иванов Иван Иванович",
      "phone": "+7 123 456 7890",
      "queue_number": 1,  ← Номер в очереди у врача #2
      ...
    },
    {
      "id": 14,
      "full_name": "Петров Петр Петрович",
      "phone": "+7 987 654 3210",
      "queue_number": 2,  ← Номер в очереди у врача #2
      ...
    }
  ],
  "total_count": 50
}
```

## 🔍 Просмотр очереди конкретного врача

### Получить очередь врача

**Endpoint:** `GET /queue/doctor/{doctor_id}`

**Пример:**
```
GET /queue/doctor/2
```

**Ответ:**
```json
{
  "queue": [
    {
      "id": 1,
      "patient_id": 15,
      "doctor_id": 2,
      "queue_number": 1,
      "queue_date": "2025-11-18",
      "patient_full_name": "Иванов Иван Иванович",
      "patient_phone": "+7 123 456 7890"
    },
    {
      "id": 2,
      "patient_id": 14,
      "doctor_id": 2,
      "queue_number": 2,
      "queue_date": "2025-11-18",
      "patient_full_name": "Петров Петр Петрович",
      "patient_phone": "+7 987 654 3210"
    }
  ],
  "total_count": 2
}
```

## ❓ Частые вопросы

### Q: Что если пациент уже зарегистрирован?
**A:** Система автоматически проверяет дубликаты по ФИО и дате рождения. Если пациент уже существует, вы получите ошибку:
```json
{
  "detail": "Patient already exists"
}
```

### Q: Нужно ли вручную добавлять пациента в очередь?
**A:** **НЕТ!** Это происходит автоматически при создании приема (`POST /appointments/`).

### Q: Нужно ли указывать врача при регистрации пациента?

**A:** **НЕТ!** При регистрации пациента (`POST /patients/`) врач НЕ указывается. Врач указывается позже при создании приема (`POST /appointments/`).

### Q: Как работает нумерация очередей у разных врачей?
**A:** Каждый врач имеет **независимую** очередь:
```
Врач #1: 1, 2, 3
Врач #2: 1, 2, 3
НЕ: 1, 2, 3, 4, 5, 6
```

### Q: Когда сбрасываются очереди?
**A:** Автоматически каждый день в **00:00**. Старые очереди удаляются.

### Q: Куда попадает новый пациент в очереди?

**A:** **В конец очереди** (FIFO). Если у врача уже 6 пациентов в очереди, новый получит номер 7.

### Q: Можно ли вручную добавить пациента в очередь?

**A:** Да, если нужно. Используйте `POST /queue/`:

```json
{
  "patient_id": 15,
  "doctor_id": 2
}
```

Но обычно это **не требуется** - пациенты добавляются автоматически при создании приема.

### Q: Как удалить пациента из очереди?

**A:** Используйте `DELETE /queue/{queue_id}`. Номера остальных пациентов автоматически пересчитаются.

## 🎓 Примеры работы

### Пример 1: Регистрация нового пациента

```
1. Reception регистрирует Иванова (БЕЗ указания врача)
   POST /patients/
   {
     "full_name": "Иванов",
     "birth_date": "1990-01-01",
     "gender": "male",
     "phone": "+7 123 456 7890"
   }

   Результат: Пациент создан, ID = 15

2. Reception создает прием к врачу Петрову (ID: 2)
   POST /appointments/
   {
     "patient_id": 15,
     "doctor_id": 2,
     "date": "2025-11-18",
     "notes": "Первичный осмотр",
     "cost": 1500
   }

3. Система автоматически создает очередь:
   Врач Петров (ID: 2): Иванов получает следующий номер (например, №7 если у врача уже 6 пациентов)

4. Reception открывает список пациентов
   GET /patients/?skip=0&limit=10

5. Иванов показывается ПЕРВЫМ в списке (не на последней странице!)
```

### Пример 2: Проверка очереди врача

```
1. Врач спрашивает: "Кто в моей очереди?"

2. Reception проверяет:
   GET /queue/doctor/2

3. Система показывает:
   1. Иванов
   2. Петров
   3. Сидоров
```

## 💡 Советы

1. **Сначала регистрируйте пациента**, затем создавайте прием к врачу
2. **Используйте параметр doctor_id** при просмотре пациентов, чтобы видеть номера очереди
3. **Последний пациент всегда первый** в списке - не листайте до конца
4. **Не добавляйте вручную** пациентов в очередь после создания приема - это происходит автоматически
5. **Очереди обнуляются** каждую ночь в 00:00
6. **Пациент добавляется в конец очереди** (FIFO) при создании приема

## 🆘 Поддержка

Если что-то работает не так:
1. Проверьте документацию: [QUEUE_README.md](QUEUE_README.md)
2. Посмотрите примеры: [CHANGELOG_QUEUE.md](CHANGELOG_QUEUE.md)
3. Свяжитесь с технической поддержкой
